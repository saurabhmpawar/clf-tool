package com.gcek.clf.tool.utility.jobs;
/*
import java.util.Date;

import org.apache.log4j.Logger;
import org.quartz.Job;
import org.quartz.JobExecutionContext;
import org.quartz.JobExecutionException;

public class JobA implements Job {

	static final Logger LOGGER = Logger.getLogger(JobA.class);

	@Override
	public void execute(JobExecutionContext context) throws JobExecutionException {
		LOGGER.info("Job A  update blob storage is runing at " + new Date());

			LOGGER.info("Job A  update blob storage is completed at " + new Date());
	}

}*/
